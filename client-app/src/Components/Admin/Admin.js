export default function Admin(props) {
  props.funcNav(false);

  return (
    <div>
        <form>
            <input type="text"></input>
            <input ></input>
        </form>
      <h1>ADMIN page</h1>
    </div>
  );
}
